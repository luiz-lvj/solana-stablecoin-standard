[toolchain]
anchor_version = "0.32.1"

[features]
resolution = true
skip-lint = false

[programs.localnet]
sss_core = "4ZFzYcNVDSew79hSAVRdtDuMqe9g4vYh7CFvitPSy5DD"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "localnet"
wallet = "../../wallet.json"

[scripts]
test = "yarn ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
